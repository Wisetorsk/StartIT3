class Robot {
    constructor(name, orientation = { x: 1, y: 0 }) {
        this.name = name;
        this.direction = orientation;
    }

    action() {
        return {
            walk: true,
            turn: 
        };
    }
}