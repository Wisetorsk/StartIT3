class Board {
    constructor() {
        this.field = []; // Nested array containing cells
        this.build();
        this.xDim, this.yDim = 10;
    }

    build() {
        for (var x in arr(this.xDim)) {
            for (var y in arr(this.yDim)) {

            }
        }
    }
}